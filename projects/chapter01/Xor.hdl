/**
 * Exclusive-or gate:
 * out = not (a == b)
 */

CHIP Xor {
    IN a, b;
    OUT out;

    PARTS:
    Nand(a = a, b = b, out = xy);
    Nand(a = a, b = xy, out = t1);
    Nand(a = b, b = xy, out = t2);
    Nand(a = t1, b = t2, out = out);
}